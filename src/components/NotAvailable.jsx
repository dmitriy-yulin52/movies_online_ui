import * as React from 'react';

const NotAvailable = () => {
    return (
        <div>
            <h1 className={'not-available'}>
                No Movies available fro selected genres
            </h1>
        </div>
    );
};

export default NotAvailable;